# בריף למתכנת האפליקציה

מה האפליקציה צריכה לעשות כדי לעבוד מול מערכת VELA-X.
החוזה הטכני המלא: `APP_API.md`.

---

## נקודת ההתחלה — בנויה

```
שחקן סורק ברקוד  →  POST /api/app/v1/sessions  →  טוקן סשן  →  משם הכול
```

המסלול מחזיר טוקן שמשמש את כל שאר הקריאות. הוא **המסלול היחיד שאינו
דורש טוקן** — כאן הוא נוצר.

הוא מוגן במפתח אפליקציה בכותרת `x-app-key`. ⚠ זה שער גס בלבד: המפתח
יושב בתוך ה־APK וניתן לחילוץ. **ההגנה האמיתית היא מצב השרת** — העמדה
חייבת להיות פעילה, אסור שירוץ עליה סשן אחר, ויש מגבלת קצב לכל טלפון.

הפירוט המלא, כולל כל קודי הכשל, ב־`APP_API.md`.

### ⚠ ההחלטה שנותרה פתוחה: אימות התשלום

כרגע `paymentRef` הוא **אופציונלי**. בלעדיו הסשן נוצר ומסומן במפורש
כלא־משולם: סטטוס `authorized` וסכום אפס, ולכן אינו נספר כשעה בתשלום
ואינו מזהם את המדדים.

זה מתאים לפיילוט. **השאלה לייצור: האם השרת מאמת תשלום מול הסליקה, או
סומך על מה שהאפליקציה מדווחת?** כשהסליקה תחובר, `paymentRef` יהפוך
לחובה והשרת יאמת אותו — **החוזה מול האפליקציה לא ישתנה.**

## ההחלטה השנייה: מי מחזיק את חיבור ה־BLE

מודול המכונה הוא `slave` ומקבל **חיבור אחד בלבד**. אם שער העמדה מחזיק
אותו, האפליקציה לא תוכל להתחבר ישירות.

| מסלול | האפליקציה | יתרון | מחיר |
|---|---|---|---|
| **א׳** | מדברת רק עם הענן; השער משדר ל־BLE | כל הפעלה עוברת בשרת וניתנת לאכיפה | שינוי משמעותי באפליקציה; בלי רשת אין אימון |
| **ב׳** | ממשיכה להתחבר ישירות ב־BLE כמו היום | כמעט אין שינוי; עובד בלי רשת | השער משחרר את החיבור בזמן סשן |

⚠ **זו ההחלטה שקובעת כמה עבודה יש למתכנת.** במסלול ב׳ — כמעט כלום. במסלול
א׳ — כל שכבת ה־BLE באפליקציה מוחלפת בקריאות לענן.

---

## מה האפליקציה צריכה לממש (מסלול ב׳)

חמש קריאות. הראשונה יוצרת את הטוקן; השאר נושאות אותו ב־`Authorization: Bearer <session_token>`.

### 0. פתיחת סשן — הראשון בכל זרימה

```
POST /api/app/v1/sessions
x-app-key: <מפתח האפליקציה>

{ "machineCode": "<מהברקוד>", "phone": "050-123-4567", "paymentRef": null }
```

מחזיר `sessionToken` **פעם אחת בלבד**. שמרו אותו לכל אורך הסשן — הוא
אינו ניתן לשחזור.

⚠ `machineCode` מקבל מזהה מכונה, טוקן QR או קוד עמדה. הברקוד מודבק על
המכונה, אך המכונה מוחלפת והעמדה נשארת.

### 1. משיכת הגדרות המכונה

```
GET /api/app/v1/session
```

מחזיר את מצב הסשן ואת **הגדרות המכונה מתורגמות** — מהירות, תדירות, סיבוב,
מצב הגשה.

⚠ **אל תתרגמו תרגילים באפליקציה.** השרת עושה זאת, כולל אימות טווחים. זו
שכבת בטיחות ואסור שתהיה תלויה בגרסת אפליקציה שהמשתמש מריץ.

⚠ ייתכן `machineSettings: null` עם `settingsError` מלא. **הסשן עדיין תקף**
— אין לחסום את המשתמש; יש להציג את השגיאה.

### 2. איסוף פקודות מהקונסולה

```
GET /api/app/v1/commands?deviceId=VX-DEV-1000
```

⚠ **חובה לקרוא באופן קבוע** — מומלץ כל 3 שניות בזמן סשן פעיל. כאן מגיעה
"עצירה" שהמפעיל לחץ בקונסולה. בלי polling, עצירה מרחוק לא תעבוד.

הפקודות מסודרות לפי עדיפות; עצירה תמיד ראשונה.

### 3. דיווח תוצאה

```
POST /api/app/v1/commands
{ "deviceId": "...", "commandId": "...", "success": true }
```

⚠ **חובה לדווח.** בלי זה הקונסולה מציגה "ממתינה" לנצח והמפעיל לא יודע
אם הפקודה בוצעה.

### 4. טלמטריה

```
POST /api/app/v1/telemetry
{ "deviceId": "...", "frames": ["BB03430040B5"], "rssi": -58 }
```

⚠ **שלחו את מסגרות ה־notify גולמיות. אל תפענחו באפליקציה.** הפענוח בשרת
כדי שיהיה מקור אמת אחד, ותיקון באג לא ידרוש עדכון אפליקציה אצל כולם.

---

### 5. תיעוד חבטות

```
POST /api/app/v1/shots
{ "deviceId": "...", "shots": [ { "sequence": 1, "firedAt": "...", "velocity": 120, "speedKmh": 30, ... } ] }
```

⚠ **בקבוצות, לא חבטה־חבטה.** בקצב של 20 לדקה שליחה בודדת הייתה 20 בקשות
לדקה. עד 500 בכל פעם.

⚠ **`sequence` רץ וייחודי בתוך הסשן — אל תאפסו אותו באמצע.** הוא הבסיס
למניעת כפילות: מנה שנשלחת שוב אחרי כשל רשת לא תיצור שורות נוספות.

⚠ **הפרידו בין הערך שנשלח למכונה לערך שגזרתם מהכיול.** `velocity` הוא מה
שנשלח; `speedKmh` הוא מה שאתם מפרשים. שני שדות שונים, נשמרים בנפרד.

## דברים שיפתיעו אתכם

**`fetched` אינו `acknowledged`.** שהאפליקציה אספה פקודה לא אומר שהמכונה
קיבלה אותה. שני מצבים נפרדים בקונסולה.

**לפקודות יש תפוגה.** `stop` חי 15 דקות, `start` רק שתיים. פקודה שפגה לא
נמסרת. זה מכוון: `start` שהגיע באיחור יפעיל מכונה שאיש לא עומד לידה.

**`lock` ו־`unlock` לא קיימים בפרוטוקול PUSUN.** אם תקבלו אותן — דווחו
`success: false`. אל תאשרו משהו שלא קרה.

**אין "השהיה" בפרוטוקול.** `pause` מתורגם לעצירה, וחידוש מתחיל מחדש.

**המכונה מדווחת שני דברים בלבד** — אחוז סוללה וקוד תקלה. אין מונה כדורים
ואין מונה זמן. אם אתם סופרים כדורים באפליקציה, זו ספירה שלכם ולא של
המכונה.

---

## מה כבר בנוי ועובד

- ארבעת המסלולים, נבדקו מקצה לקצה מול שרת רץ
- אימות: המכשיר חייב להיות משויך לסשן, אחרת 403
- תור פקודות עם עדיפויות ותפוגה
- תרגום תרגיל להגדרות מכונה, כולל אימות טווחים
- קידוד הפרוטוקול, מאומת מול כל דוגמאות היצרן

## מה חסר

1. **סליקה** — אין ספק סליקה מחובר. עד אז סשנים נוצרים כלא־משולמים.
2. **החלטת BLE** — מסלול א׳ או ב׳ (למעלה).

## מה להעביר למתכנת

1. `APP_API.md` — החוזה הטכני המלא
2. המסמך הזה
3. תשובה על שתי ההחלטות: **הנפקת טוקן** ו־**מסלול BLE**

בלי שתי התשובות הוא יכול להתחיל לקרוא, אבל לא לבנות.
