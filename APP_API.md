# חוזה ה־API לאפליקציית הטלפון

`/api/app/v1/*` — הממשק שאפליקציית VELA-X מדברת איתו.

---

## למה הממשק בנוי כך

למכונת PUSUN אין קישוריות משלה. הפרוטוקול הוא BLE בין הטלפון למכונה בלבד,
ולכן **הענן לעולם אינו מדבר עם מכונה**. האפליקציה היא הגשר היחיד.

מכאן נגזרות שלוש החלטות:

1. **פקודות ממתינות בתור** ואינן נדחפות. הקונסולה מזינה בקשה; האפליקציה
   אוספת אותה כשהיא מחוברת ב־BLE.
2. **הפענוח נעשה בשרת.** האפליקציה מעבירה מסגרות BLE גולמיות. כך יש מקור
   אמת אחד לפרוטוקול, ותיקון באג אינו דורש עדכון אפליקציה אצל המשתמשים.
3. **התרגום מתרגיל להגדרות מכונה נעשה בשרת.** אימות הטווחים הוא שכבת
   בטיחות, ואסור שיהיה תלוי בגרסת אפליקציה שהמשתמש מריץ.

---

## אימות

כל הבקשות דורשות טוקן סשן:

```
Authorization: Bearer <session_token>
```

הטוקן מונפק כשסשן משולם ונשמר במסד כ־SHA-256 בלבד. קודי הכשל:

| קוד | משמעות | מה האפליקציה צריכה לעשות |
|---|---|---|
| `missing_token` | לא נשלחה כותרת | לשלוח טוקן |
| `invalid_token` | טוקן לא מוכר | לבקש סשן חדש |
| `expired_token` | פג תוקף | לבקש סשן חדש |
| `device_mismatch` | המכשיר אינו משויך לסשן | לא לנסות שוב |

⚠ כל בקשה חייבת לכלול `deviceId`, והשרת מוודא שהוא המכשיר של אותו סשן.
בלי זה אפליקציה יכולה למשוך פקודות של מכשיר אחר.

---

## `POST /api/app/v1/sessions` — נקודת ההתחלה

**המסלול היחיד שאינו דורש טוקן סשן** — כאן הטוקן נוצר. כל השאר תלויים בו.

```
x-app-key: <מפתח האפליקציה>
```

```json
{
  "machineCode": "VX-DEV-1004",
  "phone": "050-123-4567",
  "playerName": "דנה",
  "drillVersionId": null,
  "scheduledMinutes": 60,
  "paymentRef": null
}
```

```json
{
  "ok": true,
  "sessionToken": "…",
  "expiresAt": "2026-08-26T09:55:13.915Z",
  "session": {
    "reference": "VX-260826-0001",
    "stationCode": "RAA-01-ST2",
    "deviceId": "VX-DEV-1004",
    "isPaid": false
  },
  "warning": "סשן ללא אסמכתת תשלום — לא ייספר כשעה בתשלום"
}
```

⚠ **הטוקן מוחזר פעם אחת ואינו ניתן לשחזור.** שמרו אותו לכל אורך הסשן.

⚠ `machineCode` מקבל כמה צורות: מזהה מכונה, טוקן QR של מכונה, קוד עמדה או
טוקן QR של עמדה. זה מכוון — הברקוד מודבק על המכונה, אך המכונה מוחלפת
והעמדה נשארת.

⚠ **`paymentRef` ריק = סשן פיילוט.** הוא נוצר בסטטוס `authorized` עם סכום
אפס, ולכן אינו נספר כשעה בתשלום — לא לפי סטטוס ולא לפי סכום. כשהסליקה
תחובר, השדה יהפוך לחובה והשרת יאמת אותו מול הספק. **החוזה לא ישתנה.**

### קודי כשל

| קוד | HTTP | מתי | מה לעשות |
|---|---|---|---|
| `station_busy` | 409 | כבר רץ אימון על העמדה | להציג הודעה, לא לנסות שוב |
| `station_unavailable` | 409 | העמדה מושבתת או בהתקנה | להציג הודעה |
| `unknown_machine` | 400 | הקוד אינו מוכר | לבקש סריקה חוזרת |
| `invalid_phone` | 400 | מספר אינו תקין | לתקן קלט |
| `rate_limited` | 429 | יותר מ־6 סשנים בשעה מאותו מספר | להמתין |
| `invalid_app_key` | 401 | מפתח אפליקציה שגוי | שגיאת תצורה |

⚠ **מפתח האפליקציה הוא שער גס בלבד.** הוא יושב בתוך ה־APK וניתן לחילוץ,
ולכן אינו גבול האבטחה. ההגנה האמיתית היא מצב השרת: העמדה חייבת להיות
פעילה, אסור שירוץ עליה סשן אחר, ויש מגבלת קצב לכל מספר.

---

## `GET /api/app/v1/session`

מחזיר את מצב הסשן ואת הגדרות המכונה לשידור.

```json
{
  "ok": true,
  "session": { "id": "…", "reference": "VX-260522-0002", "status": "paid", "scheduledMinutes": 60 },
  "machineSettings": {
    "serveMode": "fixed",
    "secondsBetweenBalls": 3,
    "spin": { "type": "topspin", "amount": 10 },
    "velocity": 160,
    "warnings": []
  },
  "settingsError": null
}
```

⚠ `machineSettings` עשוי להיות `null` עם `settingsError` מלא — למשל כשהתרגיל
מוגדר בקמ״ש ואין טבלת כיול. **הסשן עדיין תקף**; רק ההגדרות אינן זמינות.
אין לחסום את המשתמש במקרה כזה.

`409 not_operable` מוחזר כשהסשן במצב שאינו מאפשר הפעלה.

---

## `GET /api/app/v1/commands?deviceId=VX-DEV-1000`

אוסף פקודות ממתינות. מסודרות לפי עדיפות — **עצירה תמיד ראשונה**.

```json
{ "ok": true, "commands": [ { "id": "…", "command": "stop", "payload": null, "expiresAt": "…" } ] }
```

⚠ לכל פקודה יש תפוגה. פקודה שפג תוקפה אינה נמסרת ומסומנת `expired`.
זה מכוון: `start` שנשלח לפני עשרים דקות והגיע עכשיו יפעיל מכונה שאיש
אינו עומד לידה.

⚠ קריאה מסמנת את הפקודות כ־`fetched`. שתי קריאות מקבילות לא יקבלו את אותה
פקודה פעמיים.

---

## `POST /api/app/v1/commands`

מדווח על תוצאה.

```json
{ "deviceId": "VX-DEV-1000", "commandId": "…", "success": true }
```

⚠ **`fetched` אינו `acknowledged`.** איסוף מהתור אינו מוכיח שהמכונה קיבלה.
רק דיווח כאן סוגר את המעגל, והקונסולה מציגה את שני המצבים בנפרד.

`409 not_acknowledgeable` — הפקודה כבר נסגרה או אינה של המכשיר הזה.

---

## `POST /api/app/v1/telemetry`

```json
{
  "deviceId": "VX-DEV-1000",
  "frames": ["BB03430040B5", "BB5E01005FB5"],
  "rssi": -58,
  "ballsFired": 142
}
```

`frames` הן מסגרות ה־notify **כפי שהתקבלו**, ללא פענוח בצד האפליקציה.

```json
{
  "ok": true,
  "accepted": { "batteryPct": 67, "faultCode": "1", "faultDescription": "הגנת גלגל" },
  "rejected": []
}
```

⚠ מסגרת שנכשלת בביקורת נדחית ואינה נרשמת. רישום ערך שגוי כאילו נמדד גרוע
מהיעדר מדידה, כי הוא מזין מדדים תפעוליים. `400` מוחזר רק כשאף מדידה לא
הייתה תקינה — מסגרת פגומה אחת מתוך כמה אינה מפילה את הבקשה.

---

## מה שהממשק הזה **אינו** עושה

⚠ **אינו אוכף תשלום.** פרוטוקול PUSUN חסר אימות לחלוטין, ולכן מכונה תפעל
עבור כל לקוח BLE — גם כזה שלא עבר דרך המערכת. ראה `SECURITY_PUSUN.md`.
הממשק מספק **נראות**, לא **מניעה**.

⚠ **אינו עוצר מכונה מיידית.** `stop` הוא בקשה בתור. אם אין טלפון מחובר,
היא ממתינה. עצירת חירום אמיתית דורשת נוכחות פיזית.

---

## `POST /api/app/v1/shots`

תיעוד חבטות. **בקבוצות, לא בודדות** — בקצב של 20 חבטות לדקה שליחה בודדת
הייתה מייצרת 20 בקשות לדקה לכל סשן. עד 500 בכל פעם.

```json
{
  "deviceId": "VX-DEV-1006",
  "shots": [
    {
      "sequence": 1,
      "firedAt": "2026-08-22T18:00:00.000Z",

      "lr": 1140, "ud": 1000,
      "velocity": 120,
      "spinType": 1, "spinAmount": 15,
      "serveMode": "fixed", "pointIndex": 3,

      "speedKmh": 30, "heightLevel": 11, "angleDegrees": -30,
      "calibrationRef": "cal-2026-08"
    }
  ]
}
```

```json
{ "ok": true, "received": 3, "stored": 3, "duplicates": 0 }
```

⚠ **`sequence` חייב להיות רץ וייחודי בתוך הסשן.** הוא הבסיס למניעת
כפילות: מנה שנשלחת שוב אחרי כשל רשת לא תיצור שורות נוספות, והתשובה תראה
זאת ב־`duplicates`. **אל תאפסו אותו באמצע סשן.**

⚠ **הפרדה בין שתי קבוצות שדות:**

| קבוצה | מקור | מהימנות |
|---|---|---|
| `lr`, `ud`, `velocity`, `spinType`, `spinAmount` | מה שנשלח למכונה | ודאי — זה מה שנשלח |
| `speedKmh`, `heightLevel`, `angleDegrees` | הכיול שלכם | נגזר, לא נמדד |

הן נשמרות בעמודות נפרדות (`commanded_*` מול `derived_*`) כדי שלא
יתערבבו. חבטה בלי ערכים גזורים נרשמת במלואה — היעדר כיול אינו חוסם.

⚠ **המכונה אינה מדווחת חבטות.** פרוטוקול PUSUN מדווח אחוז סוללה וקודי
תקלה בלבד. כל מה שנרשם כאן הוא מה שהאפליקציה **פקדה**, ואין אישור שהחבטה
יצאה כפי שהוזמנה. הקונסולה מציגה זאת במפורש.

`velocity` נדחה מחוץ לטווח 80–180, `lr` מחוץ ל־210–2070, `ud` מחוץ
ל־300–4200 — אותם טווחים שהמכונה מקבלת.
