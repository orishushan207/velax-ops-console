# שליטה מרחוק במכונה — מה חוסם ומה פותר

## המצב היום: לא

הקונסולה **אינה יכולה** להפעיל או לעצור מכונה בלי שמישהו נמצא לידה.

הסיבה אינה בתוכנה. ל־PT-9001 אין WiFi, אין סלולר ואין שום קישוריות משלה.
הערוץ היחיד הוא BLE בטווח של כעשרה מטרים. **לענן אין נתיב פיזי אל המכונה.**

מה שכן קיים היום:

| פעולה | עובד? |
|---|---|
| להזין פקודה לתור | ✅ בנוי ונבדק |
| לעצור סשן שרץ, כשטלפון מחובר | ✅ הפקודה תגיע דרך האפליקציה |
| למנוע סשנים חדשים בעמדה | ✅ מיידי, בשרת |
| **להפעיל או לעצור מכונה שאיש אינו לידה** | ❌ אין נתיב — עד להתקנת שער |

הקונסולה מזינה בקשה לתור. משהו חייב לשאת אותה ב־BLE אל המכונה — היום זה
הטלפון, ולכן נדרש אדם בשטח.

---

## הפתרון: שער BLE קבוע בעמדה

מכשיר קטן שמותקן בעמדה, מחובר לחשמל ולרשת המועדון, ומחזיק קשר BLE עם
המכונה. הוא מחליף את הטלפון בתפקיד הגשר — ואז השליטה מרחוק אמיתית.

```
קונסולה  →  תור פקודות  →  שער בעמדה  →  BLE  →  מכונה
                 (בנוי)        (חסר)      (בנוי)
```

**שני שלישים מהדרך כבר בנויים.** תור הפקודות קיים, וקידוד הפרוטוקול קיים
ונבדק מול כל דוגמאות היצרן. מה שחסר הוא המכשיר עצמו, והזדהות ברמת מכשיר
ב־API — היום הוא מחייב טוקן סשן, ולשער אין סשן.

### חומרה

| אפשרות | עלות משוערת ליחידה | הערה |
|---|---|---|
| ESP32 | 30–60 ₪ | BLE + WiFi מובנים, צריכת חשמל נמוכה |
| Raspberry Pi Zero 2 W | 60–120 ₪ | חזק יותר, קל יותר לפיתוח ותחזוקה |

דרישות: חשמל בעמדה (קיים — המכונות נטענות), קליטת רשת, וטווח BLE
מהמכונה. במגרש פתוח ייתכן שיידרש מודם סלולרי במקום WiFi.

---

## בונוס: זה פותר גם את בעיית האבטחה

מפרט המודול קובע `Module type: slave module` — הוא מקבל **חיבור אחד בלבד**
בכל רגע.

כלומר שער שמחזיק את החיבור באופן קבוע **חוסם בפועל כל מתחבר אחר**. זו
הגנה אמיתית מפני שימוש עוקף, והיא אינה תלויה ב־PUSUN ואינה דורשת שינוי
Firmware.

⚠ **המחיר: האפליקציה לא תוכל להתחבר ישירות למכונה.** יש שתי דרכים:

**א. השער הוא הערוץ היחיד.** האפליקציה מדברת עם הענן, הענן עם השער, השער
עם המכונה. הכי מאובטח — כל הפעלה עוברת בשרת ולכן ניתנת לאכיפה. דורש שינוי
באפליקציה, ומוסיף תלות ברשת של המועדון: אם הרשת נופלת, אי אפשר להתחיל
אימון גם כשהשחקן עומד מול המכונה.

**ב. השער משחרר את החיבור כשמתחיל סשן.** האפליקציה ממשיכה לעבוד כמו היום,
והשער מתחבר רק בין סשנים. שומר על הזרימה הקיימת ועל עבודה גם בלי רשת, אבל
מאבד את חסימת המתחברים בזמן שהחיבור משוחרר.

---

## מה נדרש כדי להתקדם

1. ~~הזדהות ברמת שער ב־API~~ ✅ **בנוי ונבדק** — ראה להלן
2. **החלטה בין א׳ ל־ב׳** — משנה את מה שהאפליקציה צריכה לעשות
3. **קושחת השער** — לולאה שמושכת פקודות, משדרת ב־BLE, ומדווחת טלמטריה
4. **פיילוט על עמדה אחת** לפני פריסה

---

## ה־API של השער — בנוי

`/api/gateway/v1/*`. השער מזדהה במפתח ארוך־טווח:

```
Authorization: Bearer <gateway_key>
x-ble-connected: true          מצב החיבור למכונה
x-gateway-firmware: 1.0.0      גרסת הקושחה
```

⚠ המפתח נשמר כ־hash בלבד ומוצג **פעם אחת** ברישום. אינו ניתן לשחזור;
מפתח שאבד מחייב סבב מפתח. זו התנהגות מכוונת.

### `GET /api/gateway/v1/commands`

```json
{ "ok": true, "device": "VX-DEV-1002", "station": "TLV-01-ST3",
  "commands": [ { "id": "…", "command": "stop", "payload": null, "expiresAt": "…" } ] }
```

⚠ **השער אינו מציין מכשיר.** המכונה נגזרת מהעמדה שאליה הוא משויך, ולכן
שער אינו יכול למשוך פקודות של עמדה אחרת גם אם ינסה. עמדה בלי מכונה
מחזירה רשימה ריקה — מצב תקין, לא שגיאה.

### `POST /api/gateway/v1/commands`

```json
{ "commandId": "…", "success": true }
```

### `POST /api/gateway/v1/telemetry`

```json
{ "frames": ["BB03430040B5"], "rssi": -52 }
```

⚠ מגיעה **ברציפות**, לא רק בזמן סשן. הקריאה נרשמת ללא שיוך לסשן, וזה
בדיוק מה שהופך את זיהוי השימוש הבלתי מוסבר לכלי עובד.

### סימני חיים

כל בקשה מעדכנת `last_seen_at`, `ble_connected` ו־`firmware_version`.
בלי זה אי אפשר להבחין בין "אין פקודות ממתינות" לבין "השער מת" — שני
המצבים נראים זהים מהקונסולה.

---

## הערה על הטלמטריה

שער קבוע פותר בעיה נוספת: היום קריאות סוללה מגיעות רק כשטלפון מחובר, ולכן
מסך בקרת השימוש מציג "אין די נתונים" כמעט בכל העמדות. שער מדווח ברציפות —
וזה הופך את זיהוי השימוש הבלתי מוסבר ממדד תיאורטי לכלי עובד.
