# VELA-X Ops Console — מסמך ניתוח

> **TRAIN SMARTER. PERFORM BETTER.**
>
> מסמך זה מסכם את קריאת שני מסמכי המקור — `VELA-X Business Plan - RTL.pdf` (26 עמודים, יולי 2026)
> ו־`VELAXmodel.xlsx` (10 גיליונות, עודכן אוגוסט 2026) — ומתרגם אותם לדרישות מערכת.
> נכתב לפני כתיבת קוד. כל הנחה שנקבעה כאן מיושמת במערכת כהגדרה הניתנת לשינוי במסך ההגדרות,
> ולא כערך קשיח בקוד.

---

## 0. מצב הפרויקט לפני התחלה

נסרקו: `/Users/ori/Desktop/Vela-X`, `/Users/ori/Downloads`, ותיקיית הבית.

**לא נמצא קוד קיים של VELA-X.** אין `package.json`, אין schema, אין מסד נתונים, אין מערכת עיצוב.
נמצא Artifact HTML סטטי בודד (`~/Documents/Claude/Artifacts/vela-x-ops-console/index.html`) —
mockup ללא backend, ללא נתונים וללא לוגיקה. הוא אינו מהווה Stack קיים ואינו מגביל את הבחירה הטכנולוגית.

מסקנה: **אין Stack קיים לשמר.** נבחר Stack חדש לפי סעיף 4 בהנחיות (ראה `OPS_CONSOLE_ARCHITECTURE.md`).

---

## 1. סיכום המודל העסקי והתפעולי

### 1.1 מהי VELA-X

VELA-X אינה חברת מכונות. היא **רשת אימון מורשית** (`Authorized Device Network`) שמניחה עמדות
אימון פאדל במועדונים קיימים, ומוכרת לשחקן **שעת אימון** — לא מכשיר.
המכונה היא PT‑9001 (יצרן PUSUN, גרסת ELITE ממותגת), עם Firmware ייעודי, `Device ID` ייחודי,
`Session Token` חתום וסימת אפליקציה — כדי שרק מכשיר מורשה יופעל דרך VELA-X.

הנכס האמיתי לפי התוכנית: השכבה שמעל החומרה — אפליקציה, זהות שחקן, תוכן, מאמנים, נתונים והטבות.
"כל עמדה חדשה אינה רק נכס מניב. היא מוסיפה מיקום, משתמשים, תוכן, הטבות ונתונים לכל המערכת."

### 1.2 שרשרת הערך — מי משלם למי

| צד | משלם | מקבל |
|---|---|---|
| שחקן | 90 ₪ לשעה (כולל מע״מ) ל־VELA-X | שעת אימון + XP + Rewards |
| מועדון | 6,000 ₪ דמי הקמה חד־פעמיים | עמדה מותקנת + הכנסת מגרש מקושרת + ערבות Earn‑Back |
| VELA-X | 5,500 ₪ עלות עמדה רזה | 500 ₪ מרווח חומרה + זרם תרומה משעות שימוש |
| מאמן | — | עמלה מ־Referral/Retention/Homework/Content |

### 1.3 מחזור החיים התפעולי (השרשרת המרכזית של המערכת)

```
ליד מועדון (CRM)
   → הסכם + תנאי Earn-Back
      → התקנת עמדה + שיוך מכונה (Device Assignment)
         → שחקן סורק QR/NFC בעמדה
            → תשלום (Payment)
               → Session Token חתום → BLE → הפעלת מכונה
                  → Session פעיל (טיימר מקומי, עובד גם Offline)
                     → סיום + XP + Rewards + Session Recap
                        ↘ כשל בהתחלה → Ticket → SLA → זיכוי
                           → תחזוקה מונעת / החלפת מכונה
                              → מדידת Earn-Back מול הכנסת מגרש מקושרת
```

### 1.4 שלבי הפריסה (פרק 23 בתוכנית)

| שלב | זמן | אבן דרך | שער מעבר |
|---|---|---|---|
| 0 Foundation | 0–90 יום | Firmware/BLE, MVP, 6–10 עמדות פיילוט | סשן עצמאי אמין |
| 1 PMF Israel | 3–9 חודשים | 3–5 מועדונים, Retention, Earn-Back | ≥1.0–1.5 שעות/יום |
| 2 National Grid | 9–24 חודשים | 50–100 עמדות, Rewards, Coach Network | כלכלה חיובית |
| 3 Platform | 18–36 חודשים | 250+ עמדות, Marketplace, Club SaaS | הכנסה חוזרת |
| 4–6 | 3–9 שנים | בינלאומי, Scale, IPO Ready | — |

**השלכה על ה־Ops Console:** המערכת נבנית עבור שלבים 0–2. P0 חייב לתמוך בפיילוט של
5 מועדונים / 10 עמדות ולהיות נכון גם ב־100 עמדות.

---

## 2. הישויות המרכזיות

### 2.1 היררכיית התשתית — הפרדה קריטית

מסמכי המקור מערבבים לעיתים "עמדה" ו"מכונה". **המערכת מפרידה ביניהן במפורש**, כי מכונה מוחלפת
ועמדה נשארת, וכל היסטוריית ה־Uptime, ה־Earn-Back וההכנסות תלויה בעמדה ולא במכשיר:

```
Club (מועדון)
 ├── Court (מגרש)                    — יחידת ההכנסה של המועדון
 ├── Station (עמדה)                  — המיקום הפיזי הקבוע במועדון; יחידת המדידה העסקית
 │     └── DeviceAssignment          — היסטוריית: איזו מכונה הייתה בעמדה ומתי
 └── Screen (מסך מסחרי)              — Display CMS

Device (מכונה)                       — נכס פיזי נייד; Serial, Firmware, Token, מוני שעות/כדורים
```

יחס: **עמדה אחת משרתת 2.5–4 מגרשים** (המודל: 2.5; היזם ארד: 2). זו הגדרה עסקית ניתנת לשינוי.

### 2.2 מפת הישויות המלאה

| קבוצה | ישויות |
|---|---|
| זהות והרשאות | `users`, `staff_profiles`, `player_profiles`, `roles`, `permissions`, `role_permissions`, `user_roles`, `user_club_scopes`, `sessions_auth` |
| רשת | `clubs`, `club_contacts`, `club_contracts`, `club_operating_hours`, `courts`, `stations`, `screens` |
| צי מכשירים | `devices`, `device_assignments`, `device_telemetry`, `firmware_versions`, `device_firmware_history` |
| ליבה תפעולית | `sessions`, `session_events`, `session_players`, `court_bookings` |
| כספים | `payments`, `payment_attempts`, `refunds`, `chargebacks`, `settlements`, `invoices`, `coupons`, `credit_wallets`, `wallet_transactions` |
| שירות | `support_tickets`, `ticket_events`, `sla_policies` |
| תחזוקה ומלאי | `maintenance_plans`, `maintenance_tasks`, `checklists`, `checklist_items`, `checklist_submissions`, `inventory_items`, `inventory_locations`, `inventory_movements`, `suppliers` |
| Earn-Back | `earn_back_agreements`, `earn_back_conditions`, `earn_back_measurements`, `earn_back_adjustments` |
| מאמנים | `coaches`, `coach_attributions`, `coach_commissions` |
| תוכן | `drills`, `drill_versions`, `programs`, `program_versions`, `homework_assignments` |
| Rewards | `rewards_accounts`, `rewards_transactions`, `challenges`, `referrals`, `subscriptions` |
| CRM | `leads`, `crm_activities`, `tasks` |
| Display | `screen_campaigns`, `media_assets` |
| מערכת | `business_settings`, `setting_versions`, `metric_definitions`, `notifications`, `automation_rules`, `audit_logs`, `files`, `consents` |

### 2.3 מחזור החיים של Session — 13 מצבים

`draft → awaiting_payment → paid → authorized → connecting → active ⇄ paused → completed`

מסלולי כשל: `failed_to_start`, `interrupted`, `cancelled`, `partially_refunded`, `fully_refunded`, `disputed`.

**כלל עסקי מהתוכנית (פרק 5.2, 15.3):** `Start Success ≥ 95%` — אחוז הסשנים בתשלום שהתחילו
ללא עזרת צוות. סשן שעבר ל־`failed_to_start` הוא מונה שלילי של המדד הזה **ומפעיל בדיקת זיכוי אוטומטי**.

**מגבלה קשיחה:** אימון אחד = שחקן אחד או שניים בלבד (`session_players` עם `CHECK` ברמת DB).

---

## 3. תהליכי העבודה המרכזיים

### 3.1 Session → תשלום → הפעלה
1. שחקן סורק QR/NFC בעמדה → `sessions` נוצר ב־`draft`.
2. תמחור: מחיר מההגדרות העסקיות בתוקף לתאריך + קופון + מע״מ 18%.
3. תשלום → `payments` + `payment_attempts`. **Idempotency key חובה** למניעת חיוב כפול.
4. הצלחה → `paid` → הנפקת `Session Token` חתום (מאוחסן מוצפן, לא מוצג ב־UI לעולם).
5. `connecting` → BLE → `active`. טיימר מקומי במכונה ממשיך גם ללא אינטרנט.
6. `completed` → XP, Rewards, Session Recap.

**כלל: זמן מ־`paid` ל־`active` שעובר את הרף (ברירת מחדל 10 דקות) → התראה + מועמד לזיכוי אוטומטי.**

### 3.2 תקלה → SLA → זיכוי
1. מקור: שחקן / צוות מועדון / טלמטריה אוטומטית / סוכן תמיכה.
2. `support_tickets` נפתח עם חומרה → SLA response + resolution deadlines מחושבים מ־`sla_policies`
   (לפי מועדון/הסכם, לא ערך גלובלי קשיח).
3. SLA ברירת מחדל מהתוכנית (פרק 14): מענה מיידי באפליקציה, אבחון תוך שעות, **תיקון 24–48 שעות
   או מכונה חלופית**, זמינות יעד **≥95%**.
4. אם התקלה קשורה ל־Session בתשלום → `Refund Rules Engine` מציע זיכוי; זיכוי מעל רף דורש מאשר.

### 3.3 תחזוקה מונעת
טריגרים: זמן / שעות עבודה / מספר Sessions / מספר כדורים / אירועים חריגים.
Checklists: יומי 60 שניות, שבועי מתועד, חודשי. צוות המועדון מסמן, מצרף תמונה ומדווח חריגה.
התוכנית קושרת זאת ל־Reward: "תפעולי על זמינות גבוהה" — ולכן ביצוע Checklist הוא **תנאי סף ב־Earn-Back**
וגם רכיב ב־Club Health Score.

### 3.4 Earn-Back — התהליך הרגיש ביותר
נמדד חודשית לכל מועדון בערבות. הפרדה מחייבת בין ארבע שכבות הכנסה:

| שכבה | מה זה | נספר ב־Earn-Back? |
|---|---|---|
| Session Revenue | מה ששולם ל־VELA-X | ❌ לא — זו הכנסת VELA-X |
| Linked Court Booking | הזמנת מגרש עם `session_id` תואם | ✅ כן |
| Incremental Revenue | הזמנה שלא הייתה מתקיימת ללא המכונה | ✅ כן — זה היעד האמיתי |
| Baseline Booking | הזמנה שהייתה מתקיימת בכל מקרה | ❌ לא |

התוכנית מודה בכך במפורש (עמ' 10): *"הכנסה זו אינה בהכרח כולה אינקרמנטלית... לכן בפיילוט יש
למדוד את החמרות של שחקנים שלא הייתה להם הזמנה."* המערכת מיישמת שדה
`incrementality_factor` ניתן להגדרה + סיווג ידני של הזמנה כ־Baseline.

### 3.5 CRM → הסכם → התקנה
13 שלבי Pipeline מ־`lead` עד `live`. חתימת הסכם יוצרת `club_contracts` + `earn_back_agreements`
+ `earn_back_conditions`, ומזמינה משימת התקנה.

---

## 4. המדדים והנוסחאות שנמצאו בקבצים

### 4.1 מדד ה־North Star
```
Paid Training Hours per Active Station per Day
  = Σ (משך Sessions בתשלום שהושלמו, בשעות)
  ÷ (מספר עמדות פעילות × ימי פעילות בתקופה)
```
פרק 12 במודל: *"המדד החשוב אינו כמה עמדות הוצבו, אלא Paid Hours per Active Station per Day."*

### 4.2 כלכלת שעת שימוש — גיליון `כלכלת יחידה` (מדויק, שוחזר במלואו)

| רכיב | תוכנית | ריאלי | שמרני | מקור |
|---|---:|---:|---:|---|
| מחיר לצרכן (כולל מע״מ) | 90.00 | 90.00 | 90.00 | `הנחות!B7` |
| בניכוי מע״מ 18% | (13.73) | (13.73) | (13.73) | `= -P × 0.18/1.18` |
| **הכנסה נטו** | **76.27** | **76.27** | **76.27** | `= P/(1+VAT)` |
| עמלת סליקה | (3.43) | (3.43) | (3.43) | `= -(P×2.7% + 1)` |
| קרן תגמולים (Rewards Reserve) | (4.58) | (4.58) | (4.58) | `= -Net × 6%` |
| עמלות מאמנים (Coach Pool) | (3.81) | (3.81) | (3.81) | `= -Net × 5%` |
| זיכויים וסיכון | (2.29) | (2.29) | (2.29) | `= -Net × 3%` |
| כדורים, בלאי ותחזוקה | (8.00) | (15.00) | (22.00) | תרחיש |
| ענן ותקשורת | (2.50) | (3.00) | (4.00) | תרחיש |
| חלפים ומתכלים | 0.00 | (3.00) | (5.00) | תרחיש |
| רזרבת אחריות והחלפה | 0.00 | (3.00) | (5.00) | תרחיש |
| **תרומה לשעה** | **51.66** | **38.16** | **26.16** | סכום |
| שיעור תרומה מהכנסה נטו | 67.7% | 50.0% | 34.3% | |

> הערה מהמודל: **שכר הטכנאי אינו בעלות המשתנה** אלא בהוצאה הקבועה, אחרת נספר פעמיים.
> זו טעות שתוקנה במודל וחייבת להישמר בקוד. `הוצאות קבועות!A47`.

### 4.3 עלות עמדה — עודכן על ידי היזם, אוגוסט 2026

| רכיב | עמדה רזה | עמדה מלאה |
|---|---:|---:|
| מכונה עד המחסן בישראל | 3,000 | 3,000 |
| נעילה, טעינה ואחסון כדורים | 800 | 3,500 |
| מסך מסחרי, נגן ומעמד | 0 | 1,800 |
| כדורים וערכת פתיחה | 900 | 1,200 |
| התקנה, הדרכה ושילוט | 800 | 500 |
| **סה״כ מותקנת** | **5,500** | **10,000** |
| מרווח מול דמי הקמה 6,000 | **+500** | **−4,000** |

### 4.4 Earn-Back — נוסחת ההחזר

```
שעות נדרשות להחזר   = מחיר כניסה ÷ הכנסת מגרש לשעה
שעות ליום נדרשות     = שעות נדרשות ÷ ימי פעילות בתקופה
```

| בסיס | מחיר כניסה | הכנסת מגרש/שעה | שעות להחזר | ימים | שעות/יום |
|---|---:|---:|---:|---:|---:|
| PDF פרק 8.5 (ישן) | 14,900 | 90 | 165.6 | 156 | **1.06** |
| מודל עדכני | 6,000 | 90 | 66.7 | 156 | **0.43** |

טבלת רגישות מה־PDF (מחיר 14,900): 80₪→186.3ש׳/1.19; 90₪→165.6/1.06; 100₪→149.0/0.96; 110₪→135.5/0.87.

### 4.5 נקודת איזון
```
עמדות נדרשות לאיזון = הוצאה שנתית קבועה ÷ (תרומה לשעה × שעות ליום × ימי פעילות בשנה)
```
בארכיטיפ רזה (730K ₪/שנה), 312 ימי פעילות, תרומה 51.66:
1.0 ש׳/יום → 45.3 עמדות · 1.5 → 30.2 · 2.0 → 22.6 · 2.5 → 18.1.

תקרת שוק ישראלי מחושבת: `מגרשים ÷ מגרשים-לעמדה × שיעור חדירה` = 350 ÷ 2.5 × 70% = **98 עמדות**.

### 4.6 מדדי איכות ופיילוט (פרק 15.3) — ספי הצלחה

| מדד | סף |
|---|---|
| שעות בתשלום לעמדה ליום | ≥ 1.5 בחודש 2 |
| חזרה תוך 30 יום | ≥ 35% |
| Start Success | ≥ 95% |
| זמינות (Uptime) | ≥ 95% |
| Refund Rate | < 3% |
| NPS | > 45 |
| החזר מועדון | מסלול ל־100% ב־180 יום |

### 4.7 תחזית 3 שנים — שתי גרסאות סותרות (ראה סעיף 5)

| מדד | PDF ש1 | PDF ש2 | PDF ש3 | XLSX ש1 | XLSX ש2 | XLSX ש3 |
|---|---:|---:|---:|---:|---:|---:|
| עמדות בסוף שנה | 30 | 100 | 250 | 25 | 55 | 70 |
| ממוצע עמדות | 15 | 65 | 175 | 12 | 40 | 62 |
| שעות/יום | 1.2 | 1.8 | 2.4 | 1.4 | 1.9 | 2.4 |
| שעות שנתיות | 5,616 | 36,504 | 131,040 | 5,242 | 23,712 | 46,426 |
| גבייה כולל מע״מ | 0.51M | 3.29M | 11.79M | 0.47M | 2.13M | 4.18M |
| תרומת שימוש | 0.29M | 1.89M | 6.77M | 0.27M | 1.23M | 2.40M |

---

## 5. סתירות והנחות שונות בין הקבצים

**כל סתירה כאן מיושמת במערכת כרשומה ב־`business_settings` עם ערך, מקור, תאריך תחולה והיסטוריה —
ולא כערך קשיח בקוד.** מסך ההגדרות מציג את שני הערכים ואת מקורם.

| # | נושא | PDF | XLSX | ההכרעה במערכת |
|---|---|---|---|---|
| 1 | **מחיר הקמה למועדון** | 14,900 ₪ (פרק 8.5, 9.1) | **6,000 ₪** (`הנחות!B36`, "עודכן לפי היזם") | ברירת מחדל **6,000** — המקור העדכני. 14,900 מתועד כערך היסטורי. משפיע ישירות על יעד Earn-Back. |
| 2 | **עלות עמדה** | 14,000 ₪ מותקנת (פרק 9.1) | 5,500 רזה / 10,000 מלאה | ברירת מחדל **5,500 (רזה)**, עם בחירת סוג עמדה לכל התקנה. |
| 3 | **עלות כדורים/שעה** | 8 ₪ (פרק 9.2) | 8 / 15 / 22 לפי תרחיש; המודל אומר ש־8 "נמוך משמעותית מהמציאות" | ברירת מחדל **8** בתרחיש "תוכנית", אך המערכת מריצה שלושה תרחישים במקביל. |
| 4 | **חלפים + רזרבת אחריות** | לא קיימים בחישוב | 0/3/5 ו־0/3/5 | נכללים כרכיבים נפרדים; ב"תוכנית" = 0. |
| 5 | **מגרשים לעמדה** | לא מוגדר במפורש | 2.5 (`ארכיטיפים!B7`), הערה: "ארד – 2 לכל עמדה" | ברירת מחדל **2.5**, ניתן לשינוי. משפיע על תקרת שוק. |
| 6 | **תחזית פריסה** | 30/100/250 עמדות | 25/55/70 | שתי התחזיות קיימות כתרחישים: `plan` = PDF, `realistic`/`conservative` = XLSX. |
| 7 | **עמלת סליקה** | 2.7% + 1 ₪ | 2.7% + 1 ₪, אך הערה: "קיבלתי תמחור על 2 שקל" ו־`G8 = 0.019` | ברירת מחדל 2.7%+1; ערך חלופי 1.9% מתועד כהצעת ספק לא חתומה. |
| 8 | **מספר ימי פעילות** | לא מוגדר | 312 בשנה, 156 בתקופת ערבות (180 יום) | 312 / 156 כהגדרות. |
| 9 | **מודל גבייה** | מודל א׳ בלבד (הקמה + לפי שעה) | שלושה מודלים: א׳ / ב׳ חודשי / ג׳ היברידי, עם המלצה על ג׳ | המערכת תומכת בשלושת סוגי ההסכם ברמת `club_contracts.pricing_model`. |
| 10 | **הפרשה ל־Earn-Back** | "רזרבה 10–15% מתקבולי ההתקנה" | `ערבות!F23 = 0` בהנחות הנוכחיות (כי 6,000 ÷ 90 = 66.7 שעות מושג בקלות) | המערכת מחשבת חשיפה בפועל מהנתונים, ומאפשרת גם Reserve % ידני וגם Exposure Cap מוחלט. |
| 11 | **עלות כדורים שהמועדון סופג** | לא מוזכר כלל בחישוב ההחזר | 20 ₪/שעה (`הנחות!B46`), "מאריך משמעותית את ההחזר האמיתי" | שדה בהסכם Earn-Back: `club_ball_cost_per_hour`, ברירת מחדל 20, ניתן לאיפוס ל־0 לפי החוזה. |
| 12 | **תרומה שנתית = רווח?** | פרק 18 מציג 6.77M תרומה בשנה 3 | המודל מדגיש: "זו לא רווח — זו תרומה לפני כל הוצאה" | **הפרדה קשיחה במערכת**: Gross Revenue / Net Revenue / Contribution / EBITDA הם ארבעה שדות שונים, לעולם לא מוצגים באותו KPI. |

---

## 6. הנחות שקבעתי (ניתנות לשינוי במסך הגדרות)

כל ההנחות נטענות ל־`business_settings` בהרצת ה־Seed, עם `effective_from` = תאריך הקמת המערכת,
`source` = המסמך שממנו נלקחו, ו־`confidence` = `verified` / `assumed` / `disputed`.

| מפתח | ערך | ביטחון | מקור |
|---|---:|---|---|
| `pricing.consumer_price_per_hour_incl_vat` | 90 ₪ | verified | PDF פרק 7 |
| `pricing.ab_test_range` | 80–100 ₪ | assumed | PDF פרק 15.1 |
| `finance.vat_rate` | 18% | verified | XLSX `הנחות!B8` |
| `finance.psp_percentage_fee` | 2.7% | verified | שניהם |
| `finance.psp_fixed_fee` | 1 ₪ | verified | שניהם |
| `finance.rewards_reserve_pct` | 6% מהכנסה נטו | verified | PDF 11.4 |
| `finance.coach_pool_pct` | 5% משוקלל | verified | PDF פרק 10 |
| `finance.refund_risk_pct` | 3% | verified | XLSX |
| `finance.operating_days_per_year` | 312 | assumed | XLSX |
| `station.installed_cost_lean` | 5,500 ₪ | verified | XLSX (עדכון יזם) |
| `station.installed_cost_full` | 10,000 ₪ | verified | XLSX |
| `station.setup_fee` | 6,000 ₪ | **disputed** | XLSX ↔ PDF 14,900 |
| `station.courts_per_station` | 2.5 | disputed | XLSX ↔ הערת ארד (2) |
| `earnback.guarantee_days` | 180 | verified | PDF 8.3 |
| `earnback.operating_days_in_period` | 156 | assumed | XLSX |
| `earnback.court_revenue_per_hour` | 90 ₪ לפני מע״מ | verified | PDF 8.5 |
| `earnback.club_ball_cost_per_hour` | 20 ₪ | assumed | XLSX בלבד |
| `earnback.incrementality_factor` | 0.70 | **assumed — דורש אימות בפיילוט** | הסקה שלי מעמ׳ 10 |
| `sla.response_hours` | 4 | assumed | PDF "אבחון תוך שעות" — לא ננקב מספר |
| `sla.resolution_hours` | 48 | verified | PDF פרק 14 |
| `sla.uptime_target_pct` | 95% | verified | PDF 14, 15.3 |
| `quality.start_success_target_pct` | 95% | verified | PDF 15.3 |
| `quality.refund_rate_alert_pct` | 3% | verified | PDF 15.3 |
| `quality.nps_target` | 45 | verified | PDF 15.3 |
| `ops.device_offline_alert_minutes` | 10 | assumed | הנחיות המשימה |
| `ops.paid_not_started_alert_minutes` | 10 | assumed | הנחיה שלי |
| `metrics.active_station_min_sessions_per_week` | 1 | verified | PDF 17.1 |
| `metrics.active_station_min_uptime_pct` | 90% | assumed | PDF אומר "מעל רף מוגדר" |
| `metrics.activated_user_sessions` | 2 בתוך 30 יום | verified | PDF 17.1 |
| `metrics.retention_windows_days` | 7 / 30 / 90 | verified | PDF פרק 17 |
| `refund.auto_credit_downtime_minutes` | 15 | assumed | הנחיה |
| `refund.approval_threshold_ils` | 200 ₪ | assumed | הנחיה |
| `coach.commission_cap_pct_per_customer` | 20% | assumed | PDF 10.2 "Cap קיים" ללא מספר |

---

## 7. אילו נתונים עדיין חסרים

### 7.1 חסר עסקי — דורש החלטה של ההנהלה
1. **מחיר ההקמה הסופי** — 6,000 או 14,900. משנה את יעד ה־Earn-Back פי 2.5.
2. **מי משלם על כדורים ובלאי** — המודל מגדיר זאת כ"החלטה על שליש מהמרווח" (`כלכלת יחידה!A29`).
3. **מודל הגבייה** — א׳ / ב׳ / ג׳. המודל ממליץ על היברידי לפיילוט.
4. **עמדה רזה או מלאה** — עמדה מלאה בעלות 10,000 מול הקמה 6,000 = הפסד של 4,000 לכל התקנה.
5. **תקרת חשיפה כוללת ל־Earn-Back** בש״ח — לא מוגדרת באף מסמך.
6. **ארכיטיפ החברה** — רזה / ביניים / ארצי. קובע את ההוצאה הקבועה ואת נקודת האיזון.
7. **טריטוריה בינלאומית** — פרק 19 מודה שאין זכויות מחוץ לישראל.

### 7.2 חסר נתוני מקור — לא קיים באף אחד מהמסמכים
- **CAC** — לא מופיע בשום מקום, אך נדרש ב־Dashboard. נבנה כשדה מחושב מ־`marketing_spend` ידני.
- **LTV / Payback Period** — נגזרים; נוסחה מיושמת אך דורשת נתוני Retention אמיתיים.
- פילוח **Peak / Off-Peak** — התוכנית דורשת מינימום שעות Off‑Peak אך לא מגדירה את החלון.
  נקבע כברירת מחדל: ראשון–חמישי 08:00–16:00 = Off‑Peak. ניתן לשינוי לכל מועדון.
- **מחירון חלקי חילוף** ועלויות ספקים.
- **תעריפי עמלה מדויקים למאמן** לכל אחד מארבעת המסלולים — התוכנית נותנת טווחים (5–10%, 15–20%).
- **נוסחת XP** — עקרונות בלבד (11.2), ללא מספרים.
- **משקלי Club Health Score** — לא קיימים; הוגדרו על ידי כטבלה ניתנת לעריכה.
- **מפרט Firmware / פרוטוקול BLE** — לא זמין; ולכן שכבת המכשיר נבנית כ־**Adapter עם Mock Provider**.
- **פרטי ספק הסליקה** — אין credentials; שכבת התשלומים נבנית כ־Adapter עם Mock Provider.

### 7.3 אינטגרציות שנבנו כ־Mock (מסומנות במפורש ב־UI)
| אינטגרציה | מצב | ממשק |
|---|---|---|
| BLE / Device Control | Mock | `DeviceControlProvider` |
| Payment Gateway | Mock | `PaymentProvider` |
| Court Booking System | Mock | `BookingProvider` |
| WhatsApp / SMS / Email / Slack | Mock | `NotificationChannel` |
| Object Storage | Local FS | `StorageProvider` |

> **כלל מחייב:** נתוני Demo מסומנים בדגל `is_demo` ברמת השורה, וה־UI מציג באנר קבוע
> `נתוני הדגמה` כאשר הם פעילים. לעולם לא מוצגים נתוני Mock כאילו הגיעו ממערכת אמיתית.

---

## 8. עקרונות שנגזרו ישירות למימוש בקוד

1. **הכנסה ≠ רווח.** ארבעה מדדים נפרדים, אף פעם לא באותו KPI.
2. **כולל מע״מ ≠ לפני מע״מ.** כל סכום במערכת נושא `amount_gross` ו־`amount_net` נפרדים.
3. **זיכוי מלא ≠ Paid Session.** סשן שזוכה במלואו יורד מכל מוני ההכנסה והשימוש.
4. **שעות וכדורים אינם הוכחת שיפור מקצועי** (PDF, כלל מוצרי עמ׳ 7). אסור להציג טענת שיפור על סמך נפח.
5. **מקסימום 2 שחקנים לאימון** — אכיפה ברמת DB ולא רק ב־UI.
6. **אין פעולה כספית ללא Audit Log ו־Transaction.**
7. **אין Secrets ב־DB או ב־UI** — `device_auth_key` נשמר מוצפן ואינו נחשף באף endpoint.
8. **נתוני בריאות/דופק/קלוריות אינם משמשים לשיווק** (PDF 13.4, 21).

---

*מסמך זה הוא נקודת האמת לכל החלטת מוצר במערכת. שינוי הנחה עסקית נעשה במסך ההגדרות ולא כאן.*
