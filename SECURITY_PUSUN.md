# ניתוח אבטחה — פרוטוקול PUSUN PT-9001

מבוסס על מסמך היצרן *"Pusun Tennis Machine Ble Communication Protocol"* ועל
צילום ה־UUID שצורף אליו. נכתב לאחר מימוש מלא של הפרוטוקול ואימותו מול כל
דוגמאות המסמך.

---

## הממצא

**הפרוטוקול אינו כולל שום מנגנון אימות.**

אין טוקן, אין חתימה, אין הצפנה, אין מפתח משותף, ואין pairing. מסגרת הפקודה
היא עשרה בתים בטקסט גלוי:

```
AA 6A 01 00 00 00 00 00 00 A5      ← "התחל להגיש כדורים"
```

כל מי שמחזיק את המסמך הזה — או שמאזין להעברה אחת — יכול לשדר את הרצף הזה
ולהפעיל את המכונה. אין למכונה דרך להבחין בין אפליקציית VELA-X לבין כלי BLE
גנרי שעולה חינם בחנות האפליקציות.

### שלוש עובדות שמחריפות את זה

1. **ה־UUID גנרי.** `0000fff0-…-00805f9b34fb` הוא המזהה המובנה של מודול
   UART של MinewSemi, ומשותף לאינספור מכשירים זולים. הוא אינו מזהה ייחודי
   ליצרן, ולא ניתן להסתמך עליו לזיהוי.

2. **אין bonding.** בצילום שצורף למסמך מופיע במפורש `NOT BONDED` — החיבור
   אינו דורש התאמה מוקדמת. כל מכשיר בטווח יכול להתחבר.

3. **הפקודה קבועה.** `AA6A01…` זהה בכל מכונה ובכל פעם. אין nonce ואין מונה,
   ולכן גם הקלטה־ושידור־חוזר פשוט עובד.

---

## מה זה אומר עסקית

התוכנית העסקית (פרק 19) מגדירה את שכבת ה־Firmware כחפיר התחרותי המרכזי,
ומזהירה: *"Bluetooth פתוח ואפליקציית ברקוד QR לבדם אינם חוסמים מכונה זהה.
ההגנה חייבת להיות ברמת PUSUN Firmware."*

**המסמך שהתקבל מראה שההגנה הזו אינה קיימת בפועל.** הנחת היסוד של המודל —
שהמכונה נשלטת רק דרך המערכת — אינה מתקיימת ברמת הפרוטוקול.

ההשלכה הישירה: **גביית תשלום לפי שעה אינה ניתנת לאכיפה טכנית במכונה.**
שחקן שמתחבר ישירות ב־BLE מקבל אימון מלא בלי לשלם, ובלי שהמערכת תדע שהתקיים.

זה נוגע לשלושה מדדים שהקונסולה מציגה — שעות בתשלום לעמדה, שיעור ההמרה
ו־Earn-Back — שכולם מניחים שכל שימוש עובר דרך המערכת.

---

## מה כן אפשר לעשות

לפי הסדר, מהאפקטיבי לפחות:

### 0. ⭐ להפעיל את סיסמת החיבור שכבר קיימת במודול — הזול והמהיר ביותר

**זו התגלית המרכזית מקריאת מפרט המודול.** ה־BLE של המכונה הוא MinewSemi
UART-C, ולמודול הזה **יש מנגנון סיסמת חיבור מובנה — והוא פשוט כבוי**.

מתוך תצורת ברירת המחדל במפרט:

```
Connection password: minew123
Connection password enable: OFF      ← כאן הבעיה
```

הפעלתו היא שתי פקודות UART מהבקר של המכונה, בזמן הייצור:

```
TTM:PWE-[1]           הפעלת חיבור מוגן בסיסמה
TTM:PWD-[XXXXXXXX]    קביעת סיסמה, עד 8 בתים
```

ובנוסף, ניתן להחליף את ה־UUID הגנרי:

```
TTM:UID-[XXXXYYYYZZZZ]    Service + RX + TX UUID
```

**מה זה נותן:** במקום שכל אפליקציית BLE תוכל להתחבר, נדרשת סיסמה. עם
סיסמה **ייחודית לכל מכונה**, מי שחילץ אחת לא פתח את הצי.

⚠ **מה זה לא נותן:** סיסמה סטטית אינה challenge–response. מי שמצליח ללכוד
את החיבור יכול לשדר אותה מחדש. אם תיקבע סיסמה אחת לכל המכונות — דליפה
אחת שוברת הכול.

עדיין, זה המעבר מ־**אפס הגנה** ל־**סוד שצריך לחלץ**, והוא דורש מ־PUSUN
שינוי תצורה ולא פיתוח. זו הבקשה שכדאי להתחיל ממנה.

### 1. לדרוש אימות ב־Firmware מהיצרן — הפתרון החזק ביותר

זו שיחה עם PUSUN, לא משימת פיתוח. המינימום הנדרש:

* **Challenge–response**: המכונה מגרילה nonce, האפליקציה מחזירה חתימה עליו
  עם מפתח פר־מכונה. `devices.auth_key_encrypted` כבר קיים במערכת בדיוק לשם כך.
* **חלון תוקף**: הרשאה שפגה מעצמה, כך שהקלטה אינה שמישה מאוחר יותר.
* **מונה עולה**: דוחה מסגרת שכבר נראתה.

בלי אלה, כל שאר הסעיפים הם האטה בלבד — לא מניעה.

### 2. שער BLE קבוע בעמדה — פותר גם שליטה מרחוק

המודול הוא **slave** ומקבל חיבור אחד בלבד בכל רגע. לכן שער BLE שמותקן
בעמדה ומחזיק את החיבור באופן קבוע **חוסם בפועל כל מתחבר אחר** — בלי
לגעת ב־Firmware ובלי תלות ביצרן.

יתרון נוסף: הוא הופך שליטה מרחוק לאפשרית באמת. ראה `REMOTE_CONTROL.md`.

### 3. נעילה פיזית

המכונה נעולה בעמדה ומשוחררת רק בסשן משולם. עוקף את הפרוטוקול לגמרי,
ואינו תלוי ביצרן. **זו ההגנה החזקה ביותר שאפשר להשיג בלי שינוי Firmware.**

### 4. גילוי במקום מניעה

המערכת אינה יכולה למנוע שימוש עוקף, אבל כן יכולה לזהות אותו:

* מונה הכדורים בטלמטריה מול הכדורים שנספרו בסשנים משולמים — פער מתמשך
  מעיד על שימוש לא מדווח.
* צריכת סוללה ללא סשן פעיל.
* קוד תקלה `אין כדורים` בלי סשן שמסביר את הריקון.

⚠ הגילוי עובד רק כשהטלמטריה מגיעה — כלומר כשטלפון מחובר. שימוש עוקף
לא ידווח על עצמו. זו הערכת תחתון, לא מדידה.

### 5. הרתעה חוזית מול המועדון

המועדון אחראי לשימוש בעמדה. זה מעביר את הסיכון לצד שנמצא פיזית במקום —
ובפועל זו ההגנה המעשית ביותר בפיילוט.

---

## מה נבנה בינתיים

`src/lib/pusun/protocol.ts` מממש את הפרוטוקול במלואו — כל 11 הפקודות,
פענוח ההודעות, ואימות טווחים לפני שידור. 23 בדיקות מאמתות אותו מול כל
דוגמה שבמסמך היצרן.

הקידוד תקין ומוכן. **מה שחסר אינו קוד אלא אימות בשכבת ה־Firmware.**

---

## הפעולה הנדרשת

לפנות ל־PUSUN עם שאלה אחת ממוקדת:

> האם ה־Firmware תומך ב־challenge–response עם מפתח פר־מכונה, ואם לא —
> האם ניתן להוסיף זאת בגרסה ייעודית ל־VELA-X?

התשובה קובעת אם מודל "תשלום לפי שעה" ניתן לאכיפה, או שצריך לעבור למודל
שאינו תלוי בכך — מנוי למועדון, או נעילה פיזית.
